<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# Release Preparation Evidence: 0.27.0

## Audit scope

- Previous tag: `v0.26.0`
- Audited head: `73114365644da1bee5e6449cccaf0143bf69841c`
- Commit count: `8`
- Ordered log SHA-256: `3b3847e6bbdabe979194c1822c2375d051d213096333982631ef8e66c73e9702`
- Subject classes: `1 feat; 6 fix; 1 test`
- Review command: `git log v0.26.0..73114365644da1bee5e6449cccaf0143bf69841c`
- Digest command: `git log --reverse --format='%H%x09%s' v0.26.0..73114365644da1bee5e6449cccaf0143bf69841c | shasum -a 256`
- Review attestation: `complete`
- Containing changes attestation: `complete`

Every commit's message and changes were reviewed, including both merge commits.
The staged contents and planned message of the containing release-preparation commit were reviewed.
The containing release-preparation commit will have the audited head as its sole parent and is the intended `v0.27.0` tag target.

## Semantic Versioning classification

- Previous version: `0.26.0`
- Chosen version: `0.27.0`
- Release date: `2026-09-08`
- Change level: `minor`

The optional cost-estimation API and Codex accounting diagnostics are additive.
The accounting fix prevents interrupted work from being charged to a later resumed turn; it preserves conservative omission where native evidence is insufficient.
Development dependencies and the test CI matrix change to satisfy security audits, while the published package retains its Node 18.3.0 runtime floor and existing runtime dependencies and peer requirements.

## Notable-change reconciliation

| Heading | Changelog key | Commit evidence |
| --- | --- | --- |
| Added | `codex:usage` | `985a2de`, `3cec085`, `3af84d9` |
| Added | `estimateCost` | `d29a7c7`, `3cec085`, `3af84d9`; DR-024 |
| Fixed | `Invalidate Codex token baselines` | `985a2de`, `3cec085` |
| Security | `Vitest` | `90d9dde` |

The table accounts for every notable change group.
The review fixes refine the unreleased estimator and diagnostic contracts, so they belong to those groups; removed investigation notes and obsolete pricing compatibility were never released and need no separate Removed entry.
Commit `c2a51fd` is non-notable test maintenance: it checks the native permission context and bounds temporary-home cleanup without changing production permission behavior.
Merge commits `1abb92c` and `7311436` add no changes beyond their respective branch tips; their accounting and security work is already reconciled above.
The containing release-preparation commit updates versions, changelog links and this evidence, with no independent product behavior.

## Pre-tag checklist

- [x] All unit tests pass — `npm test`: all 1,397 tests in 52 files passed locally with Vitest 4.1.11 and in the Node 20, 22 and 24 CI matrix. Those unchanged results are retained; the seven release-audit cases validate the new preparation before committing.
- [x] `npm run smoke:release` passes locally: requirement explicitly waived by the maintainer on 2026-09-08: "Don't wait for local installs. As long as CI is green, do the release." This checkbox records the waiver, not a passing local run. Both the local Node 25 run and the Node 22 retry timed out downloading native SDKs; further local installation attempts were discontinued at the maintainer's request. Build and package-output stages passed. The complete equivalent CI gates passed for the audited head.
- [x] `CHANGELOG.md` contains `0.27.0`, dated `2026-09-08`, with the audited changes and updated links.
- [x] `package.json` and both release-version fields in `package-lock.json` carry `0.27.0`.
- [x] All release changes through the audited head are committed; the containing release-preparation commit includes the reviewed metadata and this evidence.
- [ ] Push the release-preparation commit to `main` and require its complete CI run, including acceptance, to pass.
- [ ] Create and push tag `v0.27.0` on the containing release-preparation commit; the publication workflow must wait for that exact commit's main CI to pass before publishing.

The full local unit suite, ESLint and typecheck passed after the development dependency changes. npm production and full audits and the pnpm full audit reported zero vulnerabilities; frozen offline pnpm lock validation passed.
The audited head's complete main CI run passed, including 13 smoke tests, distributable verification, and 54 acceptance tests with 10 configured skips. The maintainer's local-smoke waiver does not waive the tagged commit's successful main CI publication gate.
Earlier spec-lint evidence remains valid because preparation changes no specs.
