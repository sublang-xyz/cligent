<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# Release Preparation Evidence: 0.26.0

## Audit scope

- Previous tag: `v0.25.0`
- Audited head: `f5c02733283c2ae0557d76ef98a78cfc514c70a5`
- Commit count: `5`
- Ordered log SHA-256: `6282955fd26097bbf2e5e3e4a1ff8962fa51380edb323ee6a50c9a579a2eb4c4`
- Subject classes: `1 docs; 1 feat; 3 fix`
- Review command: `git log v0.25.0..f5c02733283c2ae0557d76ef98a78cfc514c70a5`
- Digest command: `git log --reverse --format='%H%x09%s' v0.25.0..f5c02733283c2ae0557d76ef98a78cfc514c70a5 | shasum -a 256`
- Review attestation: `complete`
- Containing changes attestation: `complete`

Every commit's message and changes were reviewed. The feature adds model discovery; the three fixes establish Electron compatibility, bounded cleanup and stable outcomes. The documentation commit defines when CLI listings complete.
The staged changes and planned message of the containing release-preparation commit were reviewed. Its sole parent is the audited head and it is the intended `v0.26.0` tag target.

## Semantic Versioning classification

- Previous version: `0.25.0`
- Chosen version: `0.26.0`
- Release date: `2026-09-08`
- Change level: `minor`

The optional public discovery API is additive. Existing adapter execution, host results, dependency ranges and runtime compatibility floors are unchanged.

## Notable-change reconciliation

| Heading | Changelog key | Commit evidence |
| --- | --- | --- |
| Added | `discoverAgentModels` | `bd9800d`, `8cedbaf`, `e297389`, `8b68c9f`, `f5c0273`; DR-023 |

The table accounts for every notable change group.
All five commits establish the newly released discovery API; its pre-release corrections do not warrant separate Fixed entries.
The containing release-preparation commit updates versions, changelog links and this evidence, with no independent product behavior.

## Pre-tag checklist

- [x] All unit tests pass — `npm test`: the 1,323-test baseline and all 28 discovery cases have passing evidence. Unchanged results are retained; the seven release-audit cases validate this preparation before committing.
- [x] `npm run smoke:release` passes locally: clean build, package output, distributable audits and all 13 built-CLI smoke tests passed with exact Gemini 0.57.0, Kimi 0.39.1 and OpenCode 1.18.25 targets.
- [x] `CHANGELOG.md` contains `0.26.0`, dated `2026-09-08`, with the audited changes and updated links.
- [x] `package.json` and both release-version fields in `package-lock.json` carry `0.26.0`.
- [x] All release changes through the audited head are committed; the containing release-preparation commit includes the reviewed metadata and this evidence.
- [ ] Push the release-preparation commit to `main` and require its CI run to pass — delegated to the release coordinator.
- [ ] Create and push tag `v0.26.0` — delegated to the release coordinator.

The release gate ran once on the new version metadata. Earlier typecheck, ESLint and spec-lint results remain valid; no source, test configuration or dependency changed during preparation.
Discovery's task-free Claude/Codex and Electron probes also passed. Real provider execution was unchanged and was not repeated locally; the preparation commit still requires the complete CI matrix and acceptance gate before tagging.
